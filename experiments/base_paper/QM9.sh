#!/bin/bash
# QM9 experiment — BROKEN / NOT AVAILABLE.
#
# The QM9 experiment files (experiments/base_paper/regression/QM9/main_QM.py and its
# configs) were lost in the repository migration and have no current equivalent, so
# this experiment cannot be run. The dataset splits were preserved. This script is
# kept as a placeholder and intentionally exits non-zero with a notice.
echo "QM9 experiment files were lost in the repo migration; splits preserved at src/simplegnn/datasets/splits/tu_splits/QM9_splits.json" >&2
exit 1
